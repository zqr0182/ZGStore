@model ZG.Store.Presentation.ViewModels.ProductListViewModel

@{
    ViewBag.Title = "Products";
}

@foreach (var prod in Model.Products)
{
    Html.RenderPartial("ProductSummary", prod);
}

@Html.Action("Paging", "ChildAction", new{action = "List", controller = "Product", currentCagetory = Model.CurrentCategory, currentPageNumber = Model.CurrentPageNum, totalPages = Model.TotalPages})
